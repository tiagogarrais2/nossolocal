{
  "name": "nosso-local",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "prisma generate && next dev -p 3000",
    "dev:migrate": "prisma generate && prisma migrate deploy && next dev -p 3000",
    "build": "prisma generate && next build",
    "build:analyze": "ANALYZE=true prisma generate && next build",
    "vercel-build": "prisma generate && prisma migrate deploy && next build",
    "vercel-migrate": "prisma generate && prisma migrate deploy",
    "start": "next start -p 3000",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio",
    "db:reset": "prisma migrate reset",
    "db:seed": "tsx prisma/seed.ts",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@next-auth/prisma-adapter": "^1.0.7",
    "@prisma/client": "^5.7.0",
    "cloudinary": "^2.9.0",
    "dotenv": "^17.2.3",
    "next": "^16.1.1",
    "next-auth": "^4.24.13",
    "next-cloudinary": "^6.17.5",
    "nodemailer": "^7.0.12",
    "pg": "^8.16.3",
    "prisma": "^5.7.0",
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "react-geolocated": "^4.4.0",
    "react-imask": "^7.6.1",
    "react-select": "^5.10.2"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.18",
    "autoprefixer": "^10.4.23",
    "dotenv-cli": "^11.0.0",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.18"
  }
}
